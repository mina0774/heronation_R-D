<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.heronation">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <!-- This tag indicates that this application requires ARCore.  This results in the application
              only being visible in the Google Play Store on devices that support ARCore. -->
    <uses-feature android:name="android.hardware.camera.ar" android:required="true"/>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".measurement.MeasurementARActivity"></activity>
        <activity android:name=".login_register.RegisterAiActivity" />
        <activity android:name=".wishlist.WishlistClosetDeleteActivity" />
        <activity android:name=".login_register.IntroActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".login_register.RegisterBodyActivity" />
        <activity android:name=".wishlist.WishlistClosetItemEditActivity" />
        <activity android:name=".wishlist.WishlistClosetEditBodyInfoActivity" />
        <activity android:name=".wishlist.WishlistClosetEditBodyActivity" />
        <activity android:name=".login_register.RegisterActivity" />
        <activity android:name=".home.ItemSearchActivity" />
        <activity android:name=".login_register.loginPageActivity" />
        <activity android:name=".main.MainActivity" />
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/provider_paths" />
        </provider>
    </application>

</manifest>